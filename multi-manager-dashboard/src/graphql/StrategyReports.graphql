query MyQuery {
  StrategyReports(sort: BLOCK_DESC) {
    _id
    allocBPS
    allocated
    allocationAdded
    block
    debtPaid
    duration
    gain
    gains
    hash
    loss
    losses
    reportDate
    strategyAddress
    vaultAddress
  }
}